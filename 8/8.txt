1. В игре крестики-нолики и змейка можно отрисовывать игровое поле с помощью замыкания. То есть 
на вход функции идет номер строки, и генерируем id ячейки во вложенной функции используя внешний 
id строки.

2.
if (!("a" in window)) { // у window есть метод "а",поэтому if не выполнится
    var a = 1;
}
alert(a);              // function a() {alert(this)}
var b = function a(x) {
    x && a(--x);
};
alert(a);               // function a() {alert(this)}
function a(x) {
    return x * 2;
}
var a;
alert(a);              // вот тут не понятно, вроде а определили и должно быть undefinied но выводит function a() {alert(this)}
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);          // дальше совсем не понятно, почему b не отрабатывает 
function a() {
    alert(this);
}
a.call(null); // object window

Со 2м заданием что то совсем не понятно.
 